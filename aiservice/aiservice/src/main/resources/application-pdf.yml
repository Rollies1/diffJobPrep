# PDF Report Configuration
# Add to application.yml or include as active profile (e.g., spring.profiles.active=real-ai,pdf)

ai:
  reports:
    storage-path: ./reports  # Local directory for PDF storage
    # Future: storage-type: s3 | minio | local
