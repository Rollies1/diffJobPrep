apiVersion: v1
kind: Service
metadata:
  name: {{ include "questionservice.fullname" . }}
  labels:
    {{- include "questionservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "questionservice.selectorLabels" . | nindent 4 }}
