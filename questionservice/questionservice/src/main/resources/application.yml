spring:
  application:
    name: question-service
  datasource:
    url: jdbc:postgresql://localhost:5432/jobprep?currentSchema=question
    username: jobprep
    password: devpassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082

# Backup Database (Neon) - uncomment below and comment above if local DB fails
#spring:
#  datasource:
#    url: jdbc:postgresql://ep-twilight-hall-ab9c4zg4-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
#    username: neondb_owner
#    password: npg_5Mlz3mkHCVcb

